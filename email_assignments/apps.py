from django.apps import AppConfig


class EmailAssignmentsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'email_assignments'
    verbose_name = 'Email Assigning'
