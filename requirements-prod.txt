# Deprecated shim. Everything now lives in requirements.txt, which is correct
# for every environment (psycopg2-binary carries a `sys_platform == 'linux'`
# marker, so it installs on Render/Linux and is skipped on Windows/macOS).
#
# This file is kept only so any build config still referencing it keeps working;
# it simply re-includes the single source of truth. Prefer `-r requirements.txt`.
-r requirements.txt
