# Django and Core
Django>=6.0,<7.0
gunicorn==21.2.0
whitenoise==6.6.0

# Database
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Forms and Filtering
django-crispy-forms==2.3
crispy-bootstrap5==2024.10
django-filter==24.3

# Excel Processing
openpyxl==3.1.2

# PDF Export
reportlab==4.1.0

# DOCX Export
python-docx==1.1.2
lxml>=4.9.0

# Rich Text Editor
django-tinymce>=5.0.0

# PQD PDF merging (uses Pillow for image->PDF, PyPDF2 already listed)
# Optional: docx2pdf works only on Windows with MS Word; Linux uses libreoffice (install via apt)
docx2pdf==0.1.8 ; sys_platform == 'win32'
Pillow>=10.0.0

# Utilities
python-dateutil==2.8.2
python-dotenv==1.0.0

# AI
anthropic>=0.40.0

# File storage — Cloudflare R2 (S3-compatible) for persistent uploads.
# Activated when USE_R2=True env var is set; otherwise local filesystem is used.
django-storages[s3]==1.14.4
